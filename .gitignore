__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Local secrets — never commit
.env

# Local state (CI keeps its own copy in the Actions cache)
state/
*.db
*.db-journal

# CAN SLIM skill, cloned by the workflow (and locally for development)
vendor/
