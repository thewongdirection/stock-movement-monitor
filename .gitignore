__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Credentials — never commit
.env

# Local state: the SQLite database, the runtime overlay written by the bot, and
# anything `monitor capture` saves. Committed sample data lives in samples/.
state/
*.db
*.db-wal
*.db-shm
*.db-journal
